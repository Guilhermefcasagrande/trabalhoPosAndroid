<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="br.edu.unidavi.unclewily.features.DetalheProdutoActivity">

    <ImageView
        android:id="@+id/img_cell"
        android:layout_width="128dp"
        android:layout_height="128dp"
        android:layout_marginTop="12dp"
        android:layout_centerHorizontal="true"
        android:src="@drawable/ic_free_breakfast_black_24dp"
        />

    <TextView
        android:id="@+id/label_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/img_cell"
        android:layout_centerHorizontal="true"
        tools:text="Nome do Produto"
        android:textSize="25sp"
        android:layout_margin="8dp"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/label_descricao"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/label_name"
        android:layout_centerHorizontal="true"
        tools:text="Texto padrão para descrição do produto. Texto padrão para descrição do produto. Texto padrão para descrição do produto."
        android:textSize="16sp"
        android:textAlignment="center"
        android:layout_margin="8dp"/>

    <TextView
        android:id="@+id/label_preco"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/label_descricao"
        android:layout_centerHorizontal="true"
        android:textSize="30sp"
        android:textColor="@android:color/holo_green_dark"
        tools:text="RS 3.75"
        android:textStyle="bold"/>

</RelativeLayout>
